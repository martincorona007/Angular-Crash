import { CheckUserValitatorDirective } from './check-user-valitator.directive';

describe('CheckUserValitatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckUserValitatorDirective();
    expect(directive).toBeTruthy();
  });
});
