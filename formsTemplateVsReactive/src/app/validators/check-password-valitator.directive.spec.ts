import { CheckPasswordValitatorDirective } from './check-password-valitator.directive';

describe('CheckPasswordValitatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckPasswordValitatorDirective();
    expect(directive).toBeTruthy();
  });
});
